{block content}
<h1 n:block="title">Old dudo</h1>

  {foreach $posts as $post}
  <div class="post">
    <div class="date">{$post->created_at|date:'F j, Y'}</div>
    <h2>{$post->nadpis}</h2>
    <div>{$post->perex}</div>
    <div>{$post->text}</div>
  </div>
  {/foreach}

{/block}